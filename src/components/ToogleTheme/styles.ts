import styled from 'styled-components';

export const ToggleButton = styled.div`
  position: fixed;
  bottom: 2rem;
  right: 2rem;

  display: flex;
  align-items: center;

  span { 
    color: var(--gray-500);
    display: inline-block;
    margin-right: 1rem;
  }
`;