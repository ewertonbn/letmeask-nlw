import styled from 'styled-components';

export const ToggleButton = styled.div`
  position: fixed;
  bottom: 2rem;
  right: 2rem;

  display: flex;
  align-items: center;

  span { 
    color: var(--gray-dark);
    display: inline-block;
    margin-right: 1rem;
  }
`;